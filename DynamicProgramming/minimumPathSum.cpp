#include <bits/stdc++.h>
using namespace std;

#define DPSolver ios_base::sync_with_stdio(0), cin.tie(0), cout.tie(0);

int memoization(const vector<vector<int>> &grid, int i, int j, int n, int m)
{
    // make it work
    // base case
}